# quality-kit stamped file — do not edit in the repo; change the kit instead.
line-length = 100
target-version = "py312"

[lint]
# defaults (E4/E7/E9/F) + bugbear, import sort, py-upgrade
extend-select = ["B", "I", "UP"]
