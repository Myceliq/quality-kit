{
  "format": "oxfmt",
  "format:check": "oxfmt --check",
  "lint": "oxlint --type-aware",
  "typecheck": "tsc --noEmit",
  "test:unit": "vitest run",
  "validate": "npm run format:check && npm run lint && npm run typecheck && npm run test:unit && npm run validate:repo --if-present && npm run build",
  "validate:fast": "npm run format:check && npm run lint && npm run typecheck && vitest run --changed"
}
